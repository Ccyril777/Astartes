{% extends 'base.html.twig' %}

{% block title %}Heading{% endblock %}

{% block body %}
    <h1>Heading</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ heading.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ heading.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ heading.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ heading.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('heading_index') }}">back to list</a>

    <a href="{{ path('heading_edit', {'id': heading.id}) }}">edit</a>

    {{ include('heading/_delete_form.html.twig') }}
{% endblock %}
